[project]
name = "memhub"
version = "0.1.0"
description = "Git-first personal AI memory protocol reference implementation"
requires-python = ">=3.10"
dependencies = ["pyyaml>=6.0"]

[project.scripts]
memhub = "memhub_cli.__main__:main"
