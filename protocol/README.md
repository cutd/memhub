# MemHub Protocol v0.1

This repository includes the full protocol draft in `docs/` for now.

Canonical local copy:

- `/projects/.hermes/docs/memhub-protocol-v0.1.md`

Key principle:

> MemHub is not an Obsidian replacement. It defines how AI agents should know a user through a Git-first, file-native memory repository.
